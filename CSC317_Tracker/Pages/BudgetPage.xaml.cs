using System;
using Microsoft.Maui.Controls;

namespace CSC317_Tracker.Pages
{
    public partial class BudgetPage : ContentPage
    {
        public BudgetPage()
        {
            InitializeComponent();

            // Optional: Connect to a ViewModel if you have one
            // BindingContext = new BudgetViewModel();
        }
    }
}
